import React from "react";

const NotFound = () => {
  return (
    <div
      style={{
        background: "#ae111a",
        height: "100vh",
        display: "flex",
        alignItems: "center",
        justifyContent: "center",
      }}
    >
      <h1>User Not Found</h1>
    </div>
  );
};

export default NotFound;
